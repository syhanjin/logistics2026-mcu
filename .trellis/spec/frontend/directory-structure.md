# Directory Structure

> Frontend applicability for this repository.

This firmware repository has no frontend directory, UI routes, assets, pages, or
component tree. Do not create `src/`, `components/`, `pages/`, or frontend build
tooling unless a future task explicitly adds a frontend product.

Use `.trellis/spec/backend/` for the actual STM32 firmware structure.
